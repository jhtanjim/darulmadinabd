import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "images.pexels.com","encrypted-tbn0.gstatic.com",'images.unsplash.com',      "darulmadinah.edu.in", 
],
    
  },
};

export default nextConfig;
